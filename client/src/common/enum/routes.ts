export enum Routes {
  DEFAULT = '/',
  NOT_FOUND = '/404',
  CONCERTS = '/catalog/concerts/',
  EVENT = '/event/:id/',
  ARTIST = '/artist/:id/',
  BANDS = '/artists',
  LOGIN = '/login',
  REGISTER = '/register',
  ALBUMS = NOT_FOUND,
}
